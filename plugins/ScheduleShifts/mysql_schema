# $Id: mysql_schema,v 1.1 2005/03/08 22:33:35 pudge Exp $
CREATE TABLE shifts (
    date            DATETIME,
    uid             MEDIUMINT(8),
    type            ENUM('shift', 'default'),
    shift           ENUM('morning', 'afternoon', 'evening'),
    last_changed    TIMESTAMP,
    INDEX byuser    (uid),
    INDEX bytime    (last_changed),
    INDEX byshift   (shift, uid, type)
);
